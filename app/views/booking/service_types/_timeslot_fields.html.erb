<div class="field">
	<%= f.label :add_available_timeslots? %><br>
	<%= f.time_field :time %><br>
	<% if f.object.persisted? %>
		<%= f.label :delete %><br>
		<%= f.check_box :_destroy %>
	<% end %>
</div>